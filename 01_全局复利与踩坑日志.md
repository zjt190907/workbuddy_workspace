# 全局复利与踩坑日志

> 犯过的错、踩过的坑、让简不满意的地方——记下来，不隐瞒不美化。
> 做得好的、让简满意的、值得复用的经验——也记下来，这就是复利。
> 两手都抓，每次翻看都能少走弯路、多走捷径。

---

## 记录格式

| 日期 | 类型 | 事件 | 原因分析 | 改进措施 |
|------|------|------|----------|----------|
| | 复利（好经验） | | | |
| | 踩坑（错误/不满） | | | |

---

## 踩坑记录

| 日期 | 类型 | 事件 | 原因分析 | 改进措施 |
|------|------|------|----------|----------|
| 2026-06-13 | 踩坑 | Playwright 在 WorkBuddy 定时调度环境下间歇性出现 `'PlaywrightContextManager' object has no attribute '_playwright'` 错误，手动执行正常 | 定时调度环境与交互式环境的上下文初始化差异 | 待排查：可能需要在自动化任务 prompt 中确保正确的 Python 环境激活 |
| 2026-06-13 | 踩坑 | 密码和钉钉 token 明文存储在 config.yaml | 项目初期快速实现，未考虑安全 | 后续可改为环境变量或加密存储 |
| 2026-06-13 | 踩坑 | seen_ids 每次整体覆盖，无法追踪已关闭Bug的历史 | 设计时只关注"新增检测"，未考虑状态变更 | 后续可改为增量更新，保留历史记录 |
| 2026-06-15 | 踩坑 | PROJECT_004_workbuddy_body 项目初始创建时放到了 `C:\Users\94858\WorkBuddy\2026-06-15-09-46-11\workbuddy-body\` 而非 `D:\workBuddy_workspace\PROJECT_004_workbuddy_body\`，未遵循工作区规范 | 新项目启动时未先扫描全局文档（00~03），直接在默认临时目录创建了项目 | **新项目必须先读取 `D:\workBuddy_workspace\` 下的全局文档，按 SOP 初始化项目目录，禁止在其他位置创建项目** |
| 2026-06-16 | 踩坑 | config.yaml 含明文密码和钉钉 token 且已被 git 跟踪推送至 GitHub（P0） | 初始 commit 未排除敏感配置文件 | 已通过 git filter-branch 清除历史，config.yaml 加入 .gitignore，建议轮换钉钉 token |
| 2026-06-16 | 踩坑 | 132 个 PNG 二进制文件被 git 跟踪，导致 .git 仓库膨胀至 85MB | 初始 commit 将所有文件纳入版本控制 | 经分析确认均为合法项目源素材，保留跟踪。后续注意控制大文件数量 |
| 2026-06-16 | 复利 | 首次项目审查发现 P0 敏感信息泄露 + P2 仓库膨胀问题 | 引入 04_项目审查管理.md，基于 PROJECT_005 的分层审查方法论 | 每次新项目创建和每周审查都应参考 04_项目审查管理.md
| 2026-07-08 | 踩坑 | 周度审查发现 PROJECT_006/007/008 共 3 个项目缺少 README.md，PROJECT_006 缺少 requirements.txt，3 个项目均无 .gitignore | 项目创建时未执行 Layer 1 基础规范检查清单 | **新项目创建必须完成 Layer 1 检查清单（README + requirements.txt + .gitignore）后才能视为"创建完成"**；将检查清单加入 03_项目创建sop.md |
| 2026-07-16 | 踩坑 | 顶层散落 4 个非规范文件：`2026_chip_comparison.html`、`ra2_hotkeys_wallpaper.py/png`、`nul`（Windows误创建空文件），均未按 SOP 归入 PROJECT 目录 | 生成文件时直接输出到顶层而非创建项目目录；`nul` 因 bash 中 `> nul` 在 Windows 下创建实体文件而非丢弃输出 | **所有产出文件必须归入 `PROJECT_序号_项目名` 目录**；Windows 环境下丢弃输出用 `> /dev/null` 不用 `> nul`；已创建 PROJECT_009/010 归档散落文件 |
| 2026-07-16 | 复利 | 全面整改工作区规范性：散落文件归档 + 3 个项目补齐 README/requirements + 审查仪表盘同步更新 | 简主动要求按规范检查，推动系统性整改 | 新项目创建时严格按 SOP 走：扫描全局文档 → 确定项目信息 → 创建目录 → 初始化(README+requirements+gitignore) → 记录 |

## 复利记录

| 日期 | 类型 | 事件 | 原因分析 | 改进措施 |
|------|------|------|----------|----------|
| 2026-06-13 | 复利 | 项目迁移到新工作区后一次运行成功 | 迁移前完整阅读了所有源码，理解了模块依赖关系；迁移时保留了 seen_bugs.json 状态文件；更新了自动化任务路径 | 以后迁移项目时：先通读源码→复制全部文件含状态数据→更新配置/路径→验证运行→更新自动化任务 |
